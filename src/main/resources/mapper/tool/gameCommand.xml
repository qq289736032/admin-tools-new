<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="gameCommand">

   <!-- <select id="findCommand" resultType="java.util.HashMap" parameterType="java.util.HashMap">
        select server_ids,command,status,create_name,create_by,add_time from game_command_log order by add_time desc
   </select>
  
   <insert id="addCommand" parameterType="java.util.HashMap">
        insert into game_command_log(
        	id,
        	server_ids,
        	command,
        	status,
        	create_name,
        	create_by
        )values(
        	#{id},
        	#{serverIds},
        	#{command},
        	#{status},
        	#{currentUser.loginName},
        	#{currentUser.id}
		)
    </insert> -->
    
</mapper>